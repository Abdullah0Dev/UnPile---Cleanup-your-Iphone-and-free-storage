nonisolated(unsafe) weak var 

https://shipmobilefast.com/asset-generator

https://appwaves.com

```swift
@usableFromInline
func dateFromMilliseconds(_ milliseconds: Double) throws -> Date {
  guard milliseconds.isFinite else {
    throw InvalidDateException()
  }
  let magnitude: Double = milliseconds < 0 ? -milliseconds : milliseconds
  guard magnitude <= maxJavaScriptDateMilliseconds else {
    throw InvalidDateException()
  }
  return Date(timeIntervalSince1970: milliseconds.rounded(.towardZero) / 1000.0)
}
```

1. test and make sure all funcitonlity works as expected with highest quality
2. matching it our data that it should return..
3. turn it into module 
4. implement the module into the real app
5. 